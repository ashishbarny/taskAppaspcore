
@model IEnumerable<taskApp.Domain.Entity.User>

<br />
    @Html.ActionLink("Create New", "Register", null, new { @class = "btn btn-default" })
</br>
@*<h2>Pending employees</h2>*@
@*<partial name="PendingUsers" />*@
@*await Html.PartialAsync("PendingUsers")*@
@*Html.Partial("PendingUsers","User");*@
    <br />
<h2>Pending / Active employees</h2>
<br />
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Username)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Email)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DateCreated)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Username)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Email)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DateCreated)
        </td>
        <td>
            @Html.ActionLink("DeactivateUser", "DeactivateUser", new { id = item.ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID })
        </td>
    </tr>
}

</table>
